const $=(s,c=document)=>c.querySelector(s), $$=(s,c=document)=>[...c.querySelectorAll(s)];
const progress=$('.progress'), heroMedia=$('.hero-media'), contactBg=$('.contact-bg img');
addEventListener('scroll',()=>{const y=scrollY,h=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${y/h*100}%`;if(heroMedia)heroMedia.style.transform=`translateY(${y*.14}px) scale(${1+Math.min(y/9000,.07)})`;const r=$('.contact')?.getBoundingClientRect();if(r&&r.top<innerHeight&&r.bottom>0)contactBg.style.transform=`scale(1.1) translateY(${r.top*.035}px)`},{passive:true});
const cur=$('.cursor'),dot=$('.cursor-dot');let mx=0,my=0,cx=0,cy=0;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;dot.style.transform=`translate(${mx-2}px,${my-2}px)`});(function loop(){cx+=(mx-cx)*.14;cy+=(my-cy)*.14;cur.style.transform=`translate(${cx-17}px,${cy-17}px)`;requestAnimationFrame(loop)})();$$('a,.compare').forEach(el=>{el.addEventListener('mouseenter',()=>{cur.style.width='58px';cur.style.height='58px'});el.addEventListener('mouseleave',()=>{cur.style.width='34px';cur.style.height='34px'})});
const cmp=$('#compare'),before=$('.before',cmp),handle=$('.handle',cmp);function setCompare(clientX){const r=cmp.getBoundingClientRect(),p=Math.max(0,Math.min(1,(clientX-r.left)/r.width))*100;before.style.width=p+'%';handle.style.left=p+'%'}cmp.addEventListener('pointermove',e=>{if(e.pointerType==='mouse'||cmp.hasPointerCapture(e.pointerId))setCompare(e.clientX)});cmp.addEventListener('pointerdown',e=>{cmp.setPointerCapture(e.pointerId);setCompare(e.clientX)});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.animate([{opacity:0,transform:'translateY(50px)'},{opacity:1,transform:'none'}],{duration:850,easing:'cubic-bezier(.2,.8,.2,1)',fill:'forwards'})}),{threshold:.12});$$('.project,.services-intro,.service-list,.transform-copy,.about-copy').forEach(e=>{e.style.opacity=0;io.observe(e)});
